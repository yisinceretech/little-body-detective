你是一位精通 HTML5、CSS3、JavaScript、Web Audio API、Canvas 2D、Three.js r128、解剖學視覺化與遊戲化教學 UI 的資深前端工程師。

──────────────────────────────
一、核心開發與部署規範 (Strict Requirements)
──────────────────────────────
1. 單檔完美運行：必須將 HTML、CSS、JavaScript 全部整合在單一個 .html 檔案中。
2. 無原生彈窗干擾：絕對禁止使用 alert、confirm、prompt (放上網頁會失效，改用 UI 文字提示或 Toast)。
3. 完整不分段：必須一次提供完整可以直接複製的程式碼，不要分段或省略 (不可出現「其餘程式碼略」)。
4. 零外部套件依賴：除了載入 Three.js r128 CDN 之外，不可使用其他外部 CSS/JS 庫，且音效必須由 Web Audio API 即時合成。
5. GitHub Pages 隨插即用：可直接 commit 至 GitHub 並啟用 GitHub Pages 部署。

──────────────────────────────
二、版面構圖與 16:9 儀表板視覺 (Layout)
──────────────────────────────
整體採用 16:9 桌面儀表板構圖，設計基準尺寸為 1920 × 1080 。  畫面必須滿版填滿瀏覽器 Viewport，絕對禁止出現全頁捲軸：- body margin: 0; width: 100vw; height: 100vh; overflow: hidden;   - 桌面版（寬度 ≥ 1280px）全頁嚴格禁止出現任何垂直或水平捲軸 。  主畫面採用「精緻左右雙欄比例 (38% : 62%)」：   
1. 左側控制面板（精簡緊湊型）：   - CSS 屬性限制：flex: 0 0 38%; max-width: 38%; height: 100%;   - 採用高度緊湊化垂直排版 (flex-direction: column; justify-content: space-between;)   - 卡片垂直間距控制在 8px 至 12px，內部 margins/padding 嚴格壓縮，確保所有卡片與監視器於單一 Viewport 內完全容納 。  2. 右側角色展示區（主視野區 - 已放大 10%）： >    - CSS 屬性限制：flex: 1; height: 100%; position: relative;   - 人物水平完全置中，角色圖片高度提升約 10%（約佔 Viewport 高度的 86% 至 95%，或將 characterGroup 設為 scale(1.1) 並以底部中心置中） >    - 腳部下方必須繪製半透明橢圓形地面投影 (Drop Shadow Plane)，使角色視覺自然站立並融入背景   - 必須完整顯示角色，100% 嚴禁裁切頭部、手臂或腳部  

──────────────────────────────
三、標題區
──────────────────────────────
標題位於畫面左上方，控制面板上方。
內容：
  - 主標題：「身體聲音合成器」
  - 版本文字：「v4.4」
  - 版本膠囊標籤：「2.5D 解剖互動版」
  - 副標題：「聽見身體的節奏，感受生命的律動」
視覺要求：
  - 主標題使用大尺寸粗體中文字
  - 主標題使用淡黃色至白色漸層
  - 加入紫色外陰影與柔和發光效果
  - 版本號緊接在主標題右側
  - 膠囊標籤使用半透明粉紫色背景與白色細框
  - 副標題尺寸較小，使用半透明白色

──────────────────────────────
四、左側控制面板結構
──────────────────────────────
左側控制區是一張大型玻璃擬態面板。
面板視覺：
  - 深藍紫色半透明背景
  - backdrop-filter: blur(18px)
  - 1px 至 2px 半透明白色邊框
  - 大圓角，約 20px
  - 藍紫色外發光與柔和陰影
  - 內容區塊之間使用半透明分隔線
  - 內距約 18px 至 24px
控制面板由上到下嚴格排列以下區域：
A. 監聽控制
B. 聽診模式
C. 切換角色人物下拉選單
D. 營養狀態
E. 手動 BPM 調整
F. 即時波形監視器
G. 狀態說明與操作提示

──────────────────────────────
五、監聽控制與聽診模式
──────────────────────────────
控制面板第一列分成左右兩個區塊。
左區標題：「監聽控制」
包含：
1. 「開始監聽」大型按鈕
  - 綠色漸層
  - 播放三角形圖示
  - 點擊後啟動 Web Audio API
2. 「停止監聽」大型按鈕
  - 深灰藍漸層
  - 方形停止圖示
  - 點擊後以 gain 淡出，不可產生爆音
3. 狀態指示燈
  - 綠色圓點
  - 顯示「待機中」、「監聽中」或「已停止」

右區標題：「聽診模式」
包含兩個大型模式按鈕：
1. 心臟模式
  - 心臟圖示
  - 紅色外框與紅色發光
  - 選取時高亮
2. 腸胃模式
  - 胃部或消化系統圖示
  - 綠色外框與綠色發光
  - 選取時高亮

模式按鈕必須可透過：
  - 控制面板點擊
  - 右側角色身上的器官點擊
兩種方式同步切換。

──────────────────────────────
六、營養狀態
──────────────────────────────
建立五個橫向排列的狀態按鈕：
1. 一般狀態
2. 咖啡因刺激
3. 暴糖反應
4. 高纖維消化
5. 精緻糖消化

每個按鈕包含：
  - 一個 emoji 或純 CSS 小圖示
  - 狀態名稱
  - 深色半透明背景
  - 圓角外框
  - hover 發光
  - selected 狀態使用亮藍色外框與光暈

營養狀態必須影響：
  - BPM
  - 心跳聲速度
  - 心跳力度
  - 腸胃聲密度
  - 波形振幅
  - 角色呼吸速度
  - 角色抖動幅度
  - 狀態說明文字

──────────────────────────────
七、BPM 控制
──────────────────────────────
建立 BPM 滑桿：
  - 範圍：40 至 160 BPM
  - 預設值：72 BPM
  - 即時顯示目前 BPM
  - 左端標示 40
  - 右端標示 160
  - 滑桿使用紫色至藍色漸層
  - 滑塊使用白銀色圓形
  - 心臟跳動動畫與心跳聲必須同步 BPM
  - ECG 波形移動速度也必須同步 BPM

──────────────────────────────
八、即時波形監視器
──────────────────────────────
波形區使用左右兩張 Canvas 卡片。
左側：
  - 標題：「心跳監測 ECG」
  - 淡黑紫色心電圖網格紙背景
  - 紅色 ECG 波形
  - 波形需包含類似 P、QRS、T 的節奏特徵
  - 每次 QRS 峰值須對應一次心跳聲與器官搏動
右側：
  - 標題：「腸胃蠕動 Borborygmi」
  - 深藍黑色背景
  - 青綠色網格線
  - 青色即時音訊波形
  - 波形必須呈現不規則、高低不同的腸胃聲群

兩張 Canvas：
  - 必須即時更新
  - 不可使用預先製作的靜態圖片
  - 使用 requestAnimationFrame
  - 必須支援高 DPI 顯示 (window.devicePixelRatio)
  - resize 時重新計算 Canvas 實際像素尺寸
  - 不可讓波形模糊或拉伸變形

──────────────────────────────
九、底部說明卡片
──────────────────────────────
控制面板底部放置兩張並排卡片。
左卡：「狀態說明」
列出五種營養狀態的效果與簡短說明。

右卡：「操作提示」
列出：
  - 點擊角色身上的器官可切換模式
  - 調整營養狀態可觀察聲音及波形變化
  - 停止監聽時聲音必須完全靜音
  - 所有聲音皆由 Web Audio API 即時產生

──────────────────────────────
十、右側角色展示區與背景融合規格
──────────────────────────────
1. 下拉選單 UI：
   - 左側控制面板內必須包含一個「🎭 切換角色人物」的 <select> 下拉選單。
2. 預設資料庫 (CHARACTER_PRESETS) 與精準解剖座標：
   - 程式碼頭部必須定義 JSON 資料結構，儲存各角色的圖片路徑與【精準器官相對座標】：
     * 經典 Q 版女孩：src: 'images/girl.png', 心臟 (x: -0.055, y: 0.23, scale: 0.18), 腸胃 (x: 0.0, y: -0.08, scale: 0.42)
     * Q 版醫學公仔：src: './images/doctor.png', 心臟 (x: -0.040, y: 0.18, scale: 0.20), 腸胃 (x: 0.01, y: -0.12, scale: 0.45)
     * 粉紅小精靈：src: './images/hero.png', 心臟 (x: -0.060, y: 0.25, scale: 0.16), 腸胃 (x: -0.02, y: -0.05, scale: 0.40)
3. 自動對齊與圖片背景去背融合：
   - 圖片載入時必須開啟 `transparent: true`。若圖片包含純白底色，程式碼需包含 Canvas 自動將 RGB (250-255) 純白像素轉為 Alpha 0 透明度的去背處理，確保無矩形白框邊界。
   - 切換角色時，Three.js 會自動更新角色 Texture，並將心臟 (`heartMesh`) 與腸胃 (`gutMesh`) 瞬間移動對齊至專屬的解剖座標與縮放比例，絕對不可偏離軀幹或飛到體外。
   - 角色腳下必須放置半透明黑色/紫色的橢圓形 Shadow Mesh，呈現自然站立在空間的質感。
   - 提供「上傳本機圖片...」選單項目作為備用選項。
   - 若圖片載入失敗，自動降級 (Fallback) 顯示預設動態人體剪影，確保頁面不報錯。

──────────────────────────────
十一、2.5D 解剖器官
──────────────────────────────
禁止使用 SphereGeometry、TorusGeometry 或其他簡單球體直接代替心臟或腸胃。
器官必須使用 CanvasTexture 動態繪製透明背景之精緻解剖圖，再套用到 THREE.PlaneGeometry。

嚴禁在器官後方產生白色矩形背景！材質必須開透光 (transparent: true)。

心臟設計：
  - 簡化的人類解剖心臟輪廓 (顯示左右心室與頂部主動脈弧)
  - 使用粉紅、亮紅及暗紅漸層與高光外發光
  - 可愛、乾淨、非血腥，不可畫成雙弧形情人節愛心
  - 疊加於角色胸腔偏左位置 (x: -0.055, y: 0.23)

消化系統設計：
  - 必須包含蜜桃橙色胃部、淺綠色盤繞小腸與粗綠色外包圍大腸
  - 可愛、乾淨、非血腥
  - 疊加於角色腹部中央 (x: 0.0, y: -0.08)

器官位置必須與 `characterGroup` 死鎖綁定，隨角色進行呼吸與抖動。

──────────────────────────────
十二、器官動畫
──────────────────────────────
心臟搏動不可只是整體等比例縮放：
  - 每次心跳必須包含 X 軸縮小、Y 軸伸長與輕微 Z 軸旋轉扭轉，模擬心肌收縮。
  - 每次 QRS 峰值觸發一次搏動。

腸胃動畫：
  - 胃部與腸道隨時間進行不規則波浪式膨脹與蠕動。
  - 動畫速度依營養狀態改變。

──────────────────────────────
十三、器官標籤與連接線
──────────────────────────────
右側角色外圍建立兩張浮動標籤卡（心臟卡片與腸胃卡片）：
  - 使用 SVG Overlay 即時繪製發光虛線連接至器官中心點。
  - 連接線終點必須使用 `camera.project()` 將 3D 器官世界座標動態轉換為 2D 螢幕座標，隨視角旋轉與 resize 即時更新，絕對不可偏移。

──────────────────────────────
十四、右側資訊卡
──────────────────────────────
右上角建立「目前狀態」玻璃卡；右下角建立「角色狀態」玻璃卡。
顯示模式、營養狀態、BPM、表情、情緒與即時系統時間。

──────────────────────────────
十五、聲音合成
──────────────────────────────
心臟聲使用 Web Audio API 低頻 Oscillator 與 Gain Envelope 合成 Lub-Dub 雙重節拍。
腸胃聲使用 Filtered White Noise 與 Band-pass Filter 合成 Borborygmi 咕嚕聲。
停止監聽時平滑 Gain 淡出，無爆音、無殘留聲音。

──────────────────────────────
十六、互動與操作
──────────────────────────────
支援：
  - 開始 / 停止監聽
  - 點擊 UI 或 2.5D 角色器官切換聽診部位
  - 切換 5 種營養狀態與拉動 BPM 滑桿
  - 切換角色預設或上傳本機圖片
  - 滑鼠拖曳角色進行 2.5D 角度旋轉、滾輪縮放、雙擊重置視角

──────────────────────────────
十七、RWD 規格
──────────────────────────────
全頁支援彈性響應式適應（16:9 桌面滿版，無全頁捲軸）。

──────────────────────────────
十八、程式碼品質
──────────────────────────────
區塊註解清晰、集中管理參數、正確釋放被替換之 Texture 與 Geometry 避免記憶體洩漏。

──────────────────────────────
十九、完成前自我檢查
──────────────────────────────
1. 是否單一完整 HTML 檔，且無 alert/confirm/prompt。
2. 人物是否具備腳下陰影且完美融入背景，無矩形白框。
3. 器官是否精準對齊胸腔與腹部解剖位置，不偏離。
4. 切換角色時器官座標是否自動更新對齊。
5. Web Audio 停止時是否平滑靜音防爆音。
